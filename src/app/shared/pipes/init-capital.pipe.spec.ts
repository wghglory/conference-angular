import { InitCapitalPipe } from './init-capital.pipe';

describe('InitCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new InitCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
